function plot_corr(X)
    [M,N]=size(X);
    m=M/4-1;
    for iter=1:m
        for iter2=1:N
            plot3([X(4*iter+1,),3],[1,3],[1,3],'-o','Color',color,'MarkerSize',2);
            hold on;
        end
    end
end